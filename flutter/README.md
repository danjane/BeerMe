# Flutter development area

Use this area for cross-platform UI experiments targeting Android and iOS.

Flutter may call native Kotlin/Swift code where BLE or secure storage requires platform-specific behaviour.

Use Flutter tests for widgets and unit tests for pure logic.

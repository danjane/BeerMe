# Python development area

Use this area for:

- protocol prototypes,
- cryptography experiments,
- backend/mailbox experiments,
- simulations,
- test-vector generation.

Use `pytest` for tests.

Suggested local setup:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install pytest
pytest
```

